#include "Interfaces/IVertexAttribute.hpp"

namespace NAMESPACE
{

VertexLayout getLayout()
{
    ASSERT(false);
    return (VertexLayout());
}

size_t IVertexAttribute::getSize()
{
    ASSERT(false);
    return (0);
}

}
