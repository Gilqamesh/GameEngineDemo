#include "Interfaces/IVertexAttribute.hpp"

namespace GilqEngine
{

VertexLayout layout()
{
    TRACE();
    ASSERT(false);
    return (VertexLayout());
}

size_t IVertexAttribute::size()
{
    TRACE();
    ASSERT(false);
    return (0);
}

}
